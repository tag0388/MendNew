{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "vite",
  "buildCommand": "vite build",
  "outputDirectory": "dist",
  "rewrites": [
    {
      "comment": "The app uses client-side routing, so every page path must serve index.html. Without this, opening or refreshing a URL like /project/123 returns 404.",
      "source": "/((?!api/).*)",
      "destination": "/index.html"
    }
  ]
}
